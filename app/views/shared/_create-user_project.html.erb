<% if @project.id %>
  <%= form_tag(project_user_projects_path(@project), id:"user-projects") do %>
    <% User.all.each_with_index do |user, index| %>
      <%= cl_image_tag user.photo, width: 50, height: 50, class: "teamember", id: user.id %>

      <%#= select_tag "user_#{index + 1}", options_for_select(User.all.map { |u| [u.first_name, u.id]}) %>
    <% end %>
    <br>
    <%= submit_tag "Create the project", class: "btn btn-normal" %>
  <% end %>
<% end %>
