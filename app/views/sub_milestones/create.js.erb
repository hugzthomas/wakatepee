  // Create a paragraph for the new review. But hide it.
  var new_sub_milestone = $("<%= j render 'shared/submilestone_ajax', sub_milestone: @sub_milestone, remote: true %>").hide();
  // Add it to the DOM, at the end of the existing reviews. It's still hidden.
  $('.submilestones').append(new_sub_milestone);
  // Show the new review with an animation!
  new_sub_milestone.slideDown();
  // Reset the form
  $('#submilestone-create').html("<%= j render 'shared/submilestone', sub_milestone: SubMilestone.new, project: @project, milestone: @milestone %>");

