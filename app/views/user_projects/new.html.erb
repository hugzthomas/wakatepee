

<% User.all.each do |user| %>
<%= user.first_name %>
<% end %>



<%= simple_form_for [@project, @user_project] do |f| %>
  <%= f.select :user, options_for_select(User.all.map { |u| [u.first_name, u.id]}) %>
 <%= f.submit "Edit", class: "btn btn-primary" %>
<% end %>








